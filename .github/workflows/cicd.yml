name: CI/CD Bash Script

on: [push, pull_request]

jobs:
  run-bash-script:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    # - name: Set up Bash
    #   run: |
    #     sudo apt-get update
    #     sudo apt-get install -y bash

    - name: Run / Test script
      run: ./findTranscriptionIDs.sh